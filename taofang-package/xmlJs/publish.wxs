
var isIncludes = function(item,arr){
  for (var i = 0 ; i < arr.length; i ++){
    if (arr[i] === item){
      return true;
    }
  }
  return false;
}
module.exports = {
  isIncludes: isIncludes,
};